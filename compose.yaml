services:
  hugo:
    image: ghcr.io/gohugoio/hugo:v0.165.0
    container_name: hugo-blog

    command:
      - server
      - --bind=0.0.0.0
      - --buildDrafts

    ports:
      - "1313:1313"

    volumes:
      - .:/project

    working_dir: /project
